% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ispline_boxplots.R
\name{plot_ispline_boxplots}
\alias{plot_ispline_boxplots}
\title{Plot facetted boxplots for all ispline basis columns}
\usage{
plot_ispline_boxplots(
  ispline_data,
  ispline_suffix = "_is",
  order_col = "zOrder",
  palette = "viridis",
  direction = -1,
  ncol = 3,
  outlier_size = 0.5
)
}
\arguments{
\item{ispline_data}{A data frame as returned by \code{\link{run_ispline_models}},
containing raw covariates, their spline bases (suffixed \code{_is}),
and an order column.}

\item{ispline_suffix}{A string suffix identifying your spline columns.
Default is \dQuote{_is}.}

\item{order_col}{The name of the grouping column (e.g. \dQuote{zOrder}).}

\item{palette}{One of the Viridis options (\dQuote{viridis}, \dQuote{magma},
\dQuote{plasma}, \dQuote{cividis}, etc.). Default \dQuote{viridis}.}

\item{direction}{Integer 1 or -1 to control palette direction. Default -1 (reversed).}

\item{ncol}{Number of columns in the facet wrap. Default 3.}

\item{outlier_size}{Size of the outlier points. Default 0.5.}
}
\value{
A \pkg{ggplot2} object showing one boxplot per spline term.
}
\description{
Creates a multi-facet boxplot of every ispline basis column in your data,
grouped by a specified order factor (e.g. zeta orders). Each spline term
(columns ending in \code{_is}) gets its own facet, and uses a
color-blind-friendly Viridis palette.
}
\details{
\enumerate{
\item Automatically detects all columns whose names end with \code{ispline_suffix}.
\item Pivots the data to long format for \pkg{ggplot2}.
\item Facets a boxplot for each spline variable with independent scales.
}
}
\examples{
\dontrun{
# load example data
data(bird.spec.fine)
data(bird.env.fine)

# prepare inputs
xy.bird    <- bird.spec.fine[,1:2]
spp.bird   <- bird.spec.fine[,3:102]
envir.bird <- bird.env.fine[,3:9]

# Fit & gather ispline tables
ispline_tabs_all <- run_ispline_models(
  spp_df    = spp.bird,
  env_df    = envir.bird,
  xy_df     = xy.bird,
  orders    = 2:6,
  sam       = 100,
  normalize = "Jaccard",
  reg_type  = "ispline"
)

# Facetted boxplots of all *_is columns
plot_ispline_boxplots(
  ispline_data   = ispline_tabs_all,
  ispline_suffix = "_is",
  order_col      = "zOrder",
  palette        = "viridis",
  direction      = -1,
  ncol           = 3
)
}

}
\seealso{
\code{\link{run_ispline_models}}, \code{\link{plot_ispline_lines}}
}
