% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{calc_geodist}
\alias{calc_geodist}
\title{Calculate geographic distance via Haversine formula}
\usage{
calc_geodist(
  vec_from,
  vec_to = NULL,
  coord_cols = c("centroid_lon", "centroid_lat")
)
}
\arguments{
\item{vec_from}{Numeric vector of length 2, or a named numeric vector containing
coordinates (e.g. c(lon = ..., lat = ...)).}

\item{vec_to}{Optional numeric vector of length 2 (destination coordinates).
If \code{NULL}, returns 0.}

\item{coord_cols}{Character vector of length 2 giving the names of the longitude
and latitude elements in \code{vec_from}/\code{vec_to} when those are named vectors.
Defaults to \code{c("centroid_lon", "centroid_lat")}.}
}
\value{
Numeric distance in meters (scalar).
}
\description{
Computes the Haversine distance in \strong{meters} between two coordinate pairs.
If \code{vec_to} is \code{NULL} (e.g., order = 1), returns 0.
}
\examples{
calc_geodist(c(18.4, -33.9), c(28.0, -26.2))
calc_geodist(c(centroid_lon = 18.4, centroid_lat = -33.9), NULL)
}
\keyword{internal}
