% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{calc_distance}
\alias{calc_distance}
\title{Calculate distance between sites in a coordinate table}
\usage{
calc_distance(df, site_col, vec_from, vec_to = NULL, coord_cols = c("x", "y"))
}
\arguments{
\item{df}{Data frame containing site IDs and coordinates.}

\item{site_col}{Column name in \code{df} containing site IDs.}

\item{vec_from}{Single site ID present in \code{df[[site_col]]}.}

\item{vec_to}{Optional vector of destination site IDs.}

\item{coord_cols}{Coordinate columns in \code{df} (default c("x","y") where x=lon, y=lat).}
}
\value{
Numeric distance in meters (scalar).
}
\description{
Convenience wrapper that looks up coordinates in \code{df} and computes Haversine
distance(s) in \strong{meters}.
}
\details{
\itemize{
\item If \code{vec_to} is \code{NULL} (order = 1), returns 0.
\item If \code{vec_to} is length 1, returns a single pairwise distance.
\item If \code{vec_to} is length > 1, returns the \strong{sum} of distances from \code{vec_from}
to each site in \code{vec_to} (useful as a simple multi-site aggregation).
}
}
\examples{
sites <- data.frame(site = c("A", "B", "C"), x = c(18.4, 28.0, 25.7), y = c(-33.9, -26.2, -29.1))
calc_distance(sites, "site", "A", "B")
calc_distance(sites, "site", "A", NULL)
}
\keyword{internal}
