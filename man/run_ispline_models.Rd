% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ispline_models.R
\name{run_ispline_models}
\alias{run_ispline_models}
\title{Run multiple Zeta.msgdm ispline models and return both models and combined ispline table}
\usage{
run_ispline_models(
  spp_df,
  env_df,
  xy_df,
  orders = 2:6,
  sam = 100,
  distance.type = "Euclidean",
  normalize = "Jaccard",
  reg_type = "ispline"
)
}
\arguments{
\item{spp_df}{A data frame or matrix of species incidence/abundance.}

\item{env_df}{A data frame of environmental covariates.}

\item{xy_df}{A two-column data frame or matrix of site coordinates.}

\item{orders}{Integer vector of zeta orders to fit (e.g. 2:6).}

\item{sam}{Integer; number of random samples per order (passed to \code{Zeta.msgdm}).}

\item{distance.type}{Character; distance metric for \code{Zeta.msgdm} (default \dQuote{Euclidean}).}

\item{normalize}{Character; normalization method for \code{Zeta.msgdm} (default \dQuote{Jaccard}).}

\item{reg_type}{Character; regression type for \code{Zeta.msgdm} (default \dQuote{ispline}).}
}
\value{
A named list with:
\describe{
\item{\code{zeta_gdm_list}}{A list of the fitted \code{Zeta.msgdm()} objects, named by order.}
\item{\code{ispline_table}}{A tibble with one row per sample, containing all
original covariates (including \code{distance}), the ispline bases suffixed
\code{_is}, and a \code{zOrder} column.}
}
}
\description{
Fits \code{Zeta.msgdm} models of type \dQuote{ispline} for a series of zeta-orders,
extracts the raw environmental covariates (plus distance) and their ispline bases,
and returns both the list of fitted models and one tidy data frame combining all orders.
}
\examples{
\dontrun{
data(bird.spec.fine); data(bird.env.fine)
xy   <- bird.spec.fine[,1:2]
spp  <- bird.spec.fine[,3:102]
env  <- bird.env.fine[,3:9]

out <- run_ispline_models(
  spp_df        = spp,
  env_df        = env,
  xy_df         = xy,
  orders        = 2:6,
  sam           = 100,
  normalize     = "Jaccard",
  reg_type      = "ispline"
)
names(out)
head(out$ispline_table)
}

}
\seealso{
\code{\link[zetadiv]{Zeta.msgdm}}, \code{\link[zetadiv]{Return.ispline}}
}
